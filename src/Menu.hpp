#pragma once

namespace Hangman {
    class Menu {
        public:
            static int show();
        // private:
            static void printTitle();
            static void showRules();
    };
}